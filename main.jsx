import React from "react";
import ReactDOM from "react-dom/client";
import DriftSentinel from "./drift_sentinel.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DriftSentinel />
  </React.StrictMode>
);
